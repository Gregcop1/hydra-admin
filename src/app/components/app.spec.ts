import {
  describe,
  expect,
  it,
} from 'angular2/testing';

export function main() {

  describe('App component', () => {

      it('should work', () => {
          expect(1).toBe(1);
      });
  });
};
